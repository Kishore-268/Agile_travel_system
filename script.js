function selectRoute(routeId){document.getElementById("route").value=routeId;document.getElementById("bookingForm").scrollIntoView({behavior:"smooth"});}
document.getElementById("bookingForm").addEventListener("submit",async function(event){event.preventDefault();const name=document.getElementById("name").value;const routeId=document.getElementById("route").value;const message=document.getElementById("message");try{const response=await fetch("/book",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:name,route_id:routeId})});const data=await response.json();message.textContent=data.message;if(data.success)document.getElementById("bookingForm").reset();}catch(error){message.textContent="Unable to connect to the server.";}});